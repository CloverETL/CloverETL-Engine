additional.plugin.list=

cloveretl.connection=cloveretl.connection
cloveretl.sequence=cloveretl.sequence
cloveretl.lookup=cloveretl.lookup
cloveretl.component=cloveretl.component
cloveretl.thirdparty=cloveretl.thirdparty
cloveretl.bulkloader=cloveretl.bulkloader
cloveretl.tlfunction=cloveretl.tlfunction
cloveretl.ctlfunction=cloveretl.ctlfunction
cloveretl.engine=cloveretl.engine
cloveretl.jdbc=cloveretl.jdbc

plugin.list=${cloveretl.connection},\
			${cloveretl.sequence},\
			${cloveretl.lookup},\
			${cloveretl.component},\
			${cloveretl.thirdparty},\
			${cloveretl.bulkloader},\
			${cloveretl.tlfunction},\
			${cloveretl.ctlfunction},\
			${cloveretl.jdbc},\
			${additional.plugin.list}


engine.dist=/lib/cloveretl.engine.jar

jdk.home=${env.JAVA_HOME}
tools=${jdk.home}/lib/tools.jar

obfuscate.plugin.pattern=
java.src.version=1.6

java.src.exclude=
java.src.header.file=
java.src.header.file.opensource=${dir.build-scripts}/engine.gpl.license.txt
java.src.header.file.commercial=${dir.build-scripts}/engine.commercial.license.txt

plugin.xml.xslt=

dist.name.infix=

example.archive.dir=cloverETL/examples
example.include.htmls=true
additional.example.projects=
example.projects=\
	${dir.examples}/WebSiteExamples,\
	${dir.examples}/RealWorldExamples,\
	${dir.examples}/AdvancedExamples,\
	${dir.examples}/CompanyTransactionsTutorial,\
	${dir.examples}/CTL1FunctionsTutorial,\
	${dir.examples}/CTL2FunctionsTutorial,\
	${dir.examples}/DataProfiling,\
	${dir.examples}/ExtExamples,\
	${dir.examples}/JavaExamples,\
	${dir.examples}/SimpleExamples,\
	${dir.examples}/DWHExamples,\
	${dir.examples}/CustomTLFunctions,\
	${additional.example.projects}

javadoc.limited=false

test.include=**/*Test.java
test.exclude=
