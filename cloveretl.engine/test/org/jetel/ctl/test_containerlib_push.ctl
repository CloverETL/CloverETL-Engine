integer pushElem;
integer[] pushList;

firstInput[] recordList;

function integer transform() {
	pushList = [1,2,3,4,5];
	pushElem = 6;
	push(pushList,pushElem);
	
	firstOutput record1;
	secondInput record2;
	recordList = [record1, record2];
	firstInput pushRecord;
	push(recordList, pushRecord);
	
	return 0;
}